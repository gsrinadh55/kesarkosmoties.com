{
  "name": "app-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npm --prefix backend install && npm --prefix backend run start",
    "start:backend": "npm --prefix backend run start",
    "start:frontend": "npm --prefix frontend run start",
    "build:frontend": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
